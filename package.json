{
  "name": "night.js",
  "version": "2.0.0-beta.1",
  "description": "Smart dark mode switch (based on location and time)",
  "author": "Jakub Biesiada",
  "license": "MIT",
  "main": "lib/night.cjs.js",
  "module": "lib/night.esm.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "release": "rimraf lib/* && npm run prettier && npm run lint && npm run build",
    "build": "rollup -c",
    "test": "jest --coverage",
    "watch": "npm run build -- --watch",
    "watch:test": "npm run test -- --watch",
    "lint": "eslint 'src/**/*.ts' --fix",
    "prettier": "prettier --write 'src/**/*.ts'",
    "deploy": "gh-pages -d demo/build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/JB1905/night.js.git"
  },
  "keywords": [
    "JavaScript",
    "dark",
    "night",
    "switch",
    "smart",
    "sun"
  ],
  "bugs": {
    "url": "https://github.com/JB1905/night.js/issues"
  },
  "homepage": "https://github.com/JB1905/night.js#readme",
  "dependencies": {
    "suncalc2": "^1.8.1"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^7.0.0",
    "@types/jest": "^24.9.1",
    "@typescript-eslint/parser": "^2.17.0",
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.9.0",
    "eslint-plugin-prettier": "^3.1.2",
    "gh-pages": "^2.2.0",
    "husky": "^4.2.1",
    "jest": "^25.1.0",
    "lint-staged": "^10.0.2",
    "prettier": "^1.19.1",
    "ts-jest": "^25.0.0",
    "rollup-plugin-terser": "^5.2.0",
    "rollup-plugin-typescript2": "^0.25.3",
    "typescript": "^3.7.5"
  },
  "jest": {
    "transform": {
      "^.+\\.ts?$": "ts-jest"
    }
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "src/**/*.{ts}": [
      "npm run prettier",
      "npm run lint"
    ]
  }
}
